## 20260908 (LuminMax)

- 更名: 模块正式更名为 **LuminMax**（原 LuminPro），作者 Maocat（二改）& 酷安@Yule
- 修复: **HDR 冷却期不对称**问题 — 原实现仅 active 状态有冷却期，inactive 状态无冷却导致退出 HDR 后立即重新检测，可能引发频繁切换；现两种状态均受冷却期保护
- 修复: 渐变函数 `fade_brightness` / `update_all` 使用 `seq` 命令在部分精简 ROM 上不可用的问题，改用 `while` 循环提升兼容性
- 修复: `CHECK_BRI` 中 `seq 1 10` 同样替换为固定循环，避免依赖外部命令
- 修复: 全新安装时 `customize.sh` 的 `write_config_json` / `ENSURE_DEFAULTS` 缺少 HDR 配置字段（`hdr_enter_threshold` / `hdr_exit_threshold` / `hdr_cooldown`）的问题
- 优化: WebUI 中所有模块路径统一使用 `MODULE_DIR` 常量，避免硬编码
- 优化: 日志导出文件名同步更名为 LuminMax

## V2.5-2609

- 修复: **HDR 场景下亮度忽明忽暗**的核心问题
  - 引入 HDR 状态机，持久化 active/inactive 状态，避免每次调用重复判断
  - 采用**双阈值滞后机制**：进入阈值 1.10 / 退出阈值 1.03，中间区域保持当前状态，彻底解决边界振荡
  - 冷却期从 2 秒延长至 8 秒（可配置），防止 HDR 比率短暂波动导致状态频繁切换
  - 进入 HDR 时自动平滑恢复亮度到阈值以下，避免峰值亮度停留在 HDR 场景干扰显示
  - 移除可能导致误判的 hdr_ratio 缓存机制，读不到比率时视为 1.0
- 新增: HDR 相关可配置项（`hdr_enter_threshold` / `hdr_exit_threshold` / `hdr_cooldown`）
- 新增: Web UI 中 HDR 开关下展开高级参数配置，支持实时调整阈值与冷却期
- 优化: 提取通用 `fade_brightness` 渐变函数，提升代码复用性
- 优化: 启动时自动清理 HDR 状态文件，避免旧状态残留
- 优化: WebUI 点击按钮卡顿问题 — 引入全局操作锁、后台静默执行、按钮 loading 状态、防重复点击
- 优化: WebUI 布局 — config-item 最小高度 44px、展开区域加背景色边框、header 更紧凑

## V2.4-2401

- 新增: **兼容模式 (轮询驱动)**，支持部分无法通过事件驱动监听亮度的机型，增强设备兼容性
- 修复: 遗留文件锁 (Stale Lock) 导致脚本失效的问题，引入 PID 存活检测与自动自愈机制
- 优化: 提升守护进程与系统服务的健壮性，开机及手动重启会自动刷新环境标记
- 新增: 完善 **[DEVELOPMENT.md](./DEVELOPMENT.md)** 开发文档，规范化云端 GitHub Actions 自动化构建流程
- 修复: 其他已知逻辑漏洞与 UI 细节显示优化

## V2.3-2301

- 新增: 引入 Rust 编写的 `lumipro` 二进制程序，取代 `inotifyd` 作为亮度文件监听器，内置防抖动与调试模式，提升稳定性
- 新增: HDR 内容避让功能，播放 HDR 视频时自动退让亮度提升
- 新增: 日志等级配置，支持 关闭 / 仅错误 / 警告+ / 全部，有效减少日志量
- 新增: 活动级黑名单功能，可指定应用在前台时禁止模块提升亮度
- 重构: 配置文件由多个独立文本文件合并为单一 `config.json`
- 重构: Web UI 前端框架升级为 Vue 3 + Vite，组件化重构全部页面
- 新增: Web UI 界面缩放、日志导出、设置卡片折叠、header 自动收起等功能
- 优化: Web UI 页面切换动画、亮度进度条动画、底栏导航动画
- 修复: 日志关键字过滤不生效的问题
- 修复: 修复其他已知问题

## v2.2-2208

- 新增: 黑名单功能，可在指定应用内禁止模块工作
- 新增: Web UI 日志导出、复制功能
- 优化: Web UI 外观
- 优化: 日志信息

## V2.2-2204

- 新增: `boost.sh` 脚本，支持一键将亮度提升至峰值极限
- 新增: 对于部分设备，支持在刷入时建议更改配置文件以适配
- 新增: inotifyd 的进程守护脚本，确保 inotifyd 进程的稳定性
- 新增: 支持自定义亮度文件设备路径
- 新增: 支持自定义 inotifyd 监测的事件
- 优化: Web UI 页面外观优化
- 优化: Web UI 的亮度刷新机制，优化性能
- 优化: 进程抢占机制，避免同时运行多个提升亮度的进程
- 修复: 刷入脚本中的配置继承问题

## V2.2-2202

### 核心逻辑

- 新增: `action.sh` 支持模块管理器"操作"按钮点击快速切换开关状态
- 新增: `stop.flag` 机制，支持在不重启服务的情况下拦截亮度提升逻辑
- 重构: `up.sh` 逻辑，支持日志内容比对刷新及文件大小自动截断
- 新增: 支持自动亮度休眠；支持自定义调整步数
- 新增: `restart.sh` 脚本，实现 PID 清理、Flag 重置及服务重启的原子化操作。
- 修复: 2.1 版本中，反复重启 inotifyd 造成的卡死问题
- 完善 `customize.sh` 的迁移逻辑，支持从 2101 版本自动补全缺失配置及备份目录

### WebUI

- 重构: 将配置划分为"亮度调节"、"执行策略"及"系统管理"三大板块
- 优化: 移除手动刷新按钮，实现 0.5s 频率的实时动态状态与日志监控
- 优化: "定时休眠"采用开关折叠设计，优化窄屏移动端布局
- 优化: "恢复默认"引入二次确认机制及安装时备份目录读取
